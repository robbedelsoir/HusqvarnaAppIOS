//
//  IOS_ProjectTests.swift
//  IOS-ProjectTests
//
//  Created by Docent on 17/11/2022.
//

import XCTest


//@testable import IOS_Project

final class IOS_ProjectTests: XCTestCase {

    override func setUpWithError() throws {
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }

    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        // Any test you write for XCTest can be annotated as throws and async.
        // Mark your test throws to produce an unexpected failure when your test encounters an uncaught error.
        // Mark your test async to allow awaiting for asynchronous code to complete. Check the results with assertions afterwards.
        
        
    }
    /*
    func testContentView() {
        // Set up the test by creating an instance of the view
        let view = ContentView()

        // Set the email and password values of the view
        view.email = "test@example.com"
        view.password = "password"

        // Perform the test by asserting that the email and password values are correct
        XCTAssertEqual(view.email, "test@example.com")
        XCTAssertEqual(view.password, "password")
    }

    func testNewReview() {
        // Set up the test by creating an instance of the view
        let view = NewReview()

        // Set the name and review values of the view
        view.name = "John Smith"
        view.review = "This is a great product!"

        // Perform the test by asserting that the name and review values are correct
        XCTAssertEqual(view.name, "John Smith")
        XCTAssertEqual(view.review, "This is a great product!")
    }
    */
    func testPerformanceExample() throws {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }

}
